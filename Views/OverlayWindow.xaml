<Window x:Class="PbRecoil.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:PbRecoil.Views"
        Title="Hexvyrr Macro HUD"
        Icon="pack://application:,,,/Resources/app_icon.ico"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Width="260"
        SizeToContent="WidthAndHeight"
        Left="20" Top="20"
        IsHitTestVisible="False">

    <Window.Resources>
        <converters:StatusBorderConverter x:Key="StatusBorderConverter"/>
        <converters:StatusBgConverter     x:Key="StatusBgConverter"/>
        <converters:StatusTextConverter   x:Key="StatusTextConverter"/>
        <converters:FiringColorConverter  x:Key="FiringColorConverter"/>
        <converters:SelectedSettingBorderBrushConverter x:Key="SelectedBorderConv"/>
        <converters:SelectedSettingBgBrushConverter     x:Key="SelectedBgConv"/>
        <converters:SelectedSettingForegroundConverter  x:Key="SelectedFgConv"/>
        <converters:SelectedSettingIndicatorVisibilityConverter x:Key="SelectedIndicatorConv"/>
        <converters:PresetActiveBgConverter x:Key="PresetActiveBgConv"/>
        <converters:PresetActiveFgConverter x:Key="PresetActiveFgConv"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Border CornerRadius="9"
            Background="#EE0A0E17"
            BorderBrush="{Binding IsEngineActive, Converter={StaticResource StatusBorderConverter}}"
            BorderThickness="1.5"
            Padding="10,8"
            Margin="4">
        <Border.LayoutTransform>
            <ScaleTransform x:Name="HudScaleTransform" ScaleX="1.0" ScaleY="1.0"/>
        </Border.LayoutTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="14" ShadowDepth="0" Color="#000000" Opacity="0.95"/>
        </Border.Effect>

        <StackPanel>
            <!-- ── MODE RINGKAS (Ketika Menu Settings F3 Tertutup) ──────────── -->
            <StackPanel Visibility="{Binding IsSettingsVisible, Converter={StaticResource InverseBoolToVis}}">
                <!-- Header Row -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="8" Height="8" Margin="0,0,6,0"
                                 Fill="{Binding IsFiring, Converter={StaticResource FiringColorConverter}}">
                            <Ellipse.Effect>
                                <DropShadowEffect BlurRadius="6" ShadowDepth="0"
                                                  Color="{Binding IsFiring, Converter={StaticResource FiringColorConverter}}"
                                                  Opacity="0.9"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock Text="HEXVYRR" FontWeight="ExtraBold" Foreground="#00F0FF" FontSize="11" VerticalAlignment="Center"/>
                        <TextBlock Text=" // SMART HUD" Foreground="#00FF88" FontSize="10" FontWeight="Bold" Margin="4,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Status Badge -->
                    <Border Grid.Column="1" CornerRadius="4" Padding="7,2"
                            Background="{Binding IsEngineActive, Converter={StaticResource StatusBgConverter}}"
                            BorderBrush="{Binding IsEngineActive, Converter={StaticResource StatusBorderConverter}}"
                            BorderThickness="1">
                        <TextBlock Text="{Binding StatusHeader}" 
                                   FontWeight="Black" FontSize="10"
                                   Foreground="{Binding IsEngineActive, Converter={StaticResource StatusTextConverter}}"/>
                    </Border>
                </Grid>

                <!-- Section 1 Summary Pill Badges -->
                <WrapPanel Margin="0,6,0,3">
                    <Border Background="#141B26" CornerRadius="3" Padding="6,2" Margin="0,0,4,4" BorderBrush="#212C3D" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Hold: " FontSize="9" Foreground="#8B949E"/>
                            <TextBlock Text="{Binding HoldMs, StringFormat={}{0}ms}" FontSize="9" FontWeight="Bold" Foreground="#00F0FF"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#141B26" CornerRadius="3" Padding="6,2" Margin="0,0,4,4" BorderBrush="#212C3D" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Release: " FontSize="9" Foreground="#8B949E"/>
                            <TextBlock Text="{Binding ReleaseMs, StringFormat={}{0}ms}" FontSize="9" FontWeight="Bold" Foreground="#00FF88"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>

                <!-- Section 2 Summary: Crosshair (Terpisah) -->
                <Border Background="#0E1624" CornerRadius="3" Padding="5,2" Margin="0,0,0,4" BorderBrush="#1F2E45" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🎯 Crosshair Dot:" FontSize="9" FontWeight="SemiBold" Foreground="#8B949E" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding CrosshairStatusLabel}" FontSize="9" FontWeight="Bold"
                                   Foreground="{Binding IsCrosshairVisible, Converter={StaticResource PresetActiveFgConv}}"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Footer Hotkeys Guide -->
                <TextBlock Text="[F1] ON/OFF  |  [F2] HUD  |  [F3] MENU"
                           FontSize="8.5" FontWeight="SemiBold" Foreground="#6E7681" Margin="0,1,0,0"/>
            </StackPanel>

            <!-- ── MODE MENU PENGATURAN INTERAKTIF (Ketika F3 Ditekan) ───── -->
            <StackPanel Visibility="{Binding IsSettingsVisible, Converter={StaticResource BoolToVis}}">
                <!-- Header Row -->
                <Grid Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="⚡ MACRO CONFIG" FontWeight="Black" Foreground="#00F0FF" FontSize="11" VerticalAlignment="Center"/>
                        <TextBlock Text=" // LIVE" Foreground="#00FF88" FontSize="10" FontWeight="Bold" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Close Hint Badge -->
                    <Border Grid.Column="1" CornerRadius="3" Padding="5,1" Background="#161F2E" BorderBrush="#263447" BorderThickness="1">
                        <TextBlock Text="[F3] TUTUP" FontSize="9" FontWeight="Bold" Foreground="#8B949E"/>
                    </Border>
                </Grid>

                <!-- ── SECTION 1: MACRO TIMING ─────────────────────────── -->
                <!-- Menu Item 0: Hold Time -->
                <Border CornerRadius="4" Padding="7,3" Margin="0,0,0,2.5" BorderThickness="1"
                        BorderBrush="{Binding SelectedSettingIndex, Converter={StaticResource SelectedBorderConv}, ConverterParameter=0}"
                        Background="{Binding SelectedSettingIndex, Converter={StaticResource SelectedBgConv}, ConverterParameter=0}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="► " FontWeight="Black" FontSize="9.5" Margin="0,0,2,0"
                                   Foreground="{Binding SelectedSettingIndex, Converter={StaticResource SelectedFgConv}, ConverterParameter=0}"
                                   Visibility="{Binding SelectedSettingIndex, Converter={StaticResource SelectedIndicatorConv}, ConverterParameter=0}"/>
                        <TextBlock Grid.Column="1" Text="Hold Time (Down)" FontSize="9.5" FontWeight="SemiBold"
                                   Foreground="{Binding SelectedSettingIndex, Converter={StaticResource SelectedFgConv}, ConverterParameter=0}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding HoldMs, StringFormat=◄  {0} ms  ►}" FontSize="9.5" FontWeight="Bold"
                                   Foreground="{Binding SelectedSettingIndex, Converter={StaticResource SelectedFgConv}, ConverterParameter=0}"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Menu Item 1: Release Delay -->
                <Border CornerRadius="4" Padding="7,3" Margin="0,0,0,2.5" BorderThickness="1"
                        BorderBrush="{Binding SelectedSettingIndex, Converter={StaticResource SelectedBorderConv}, ConverterParameter=1}"
                        Background="{Binding SelectedSettingIndex, Converter={StaticResource SelectedBgConv}, ConverterParameter=1}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="► " FontWeight="Black" FontSize="9.5" Margin="0,0,2,0"
                                   Foreground="{Binding SelectedSettingIndex, Converter={StaticResource SelectedFgConv}, ConverterParameter=1}"
                                   Visibility="{Binding SelectedSettingIndex, Converter={StaticResource SelectedIndicatorConv}, ConverterParameter=1}"/>
                        <TextBlock Grid.Column="1" Text="Release Delay (Up)" FontSize="9.5" FontWeight="SemiBold"
                                   Foreground="{Binding SelectedSettingIndex, Converter={StaticResource SelectedFgConv}, ConverterParameter=1}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding ReleaseMs, StringFormat=◄  {0} ms  ►}" FontSize="9.5" FontWeight="Bold"
                                   Foreground="{Binding SelectedSettingIndex, Converter={StaticResource SelectedFgConv}, ConverterParameter=1}"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- ── SECTION 2: DISPLAY / CROSSHAIR ──────────────────── -->
                <Grid Margin="0,4,0,3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Rectangle Grid.Column="0" Height="1" Fill="#1E2D42" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text=" DISPLAY " FontSize="8" FontWeight="Bold"
                               Foreground="#3B5A80" Margin="4,0" VerticalAlignment="Center"/>
                    <Rectangle Grid.Column="2" Height="1" Fill="#1E2D42" VerticalAlignment="Center"/>
                </Grid>

                <!-- Menu Item 2: Crosshair Dot (ON / OFF) -->
                <Border CornerRadius="4" Padding="7,3" Margin="0,0,0,5" BorderThickness="1"
                        BorderBrush="{Binding SelectedSettingIndex, Converter={StaticResource SelectedBorderConv}, ConverterParameter=2}"
                        Background="{Binding SelectedSettingIndex, Converter={StaticResource SelectedBgConv}, ConverterParameter=2}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="► " FontWeight="Black" FontSize="9.5" Margin="0,0,2,0"
                                   Foreground="{Binding SelectedSettingIndex, Converter={StaticResource SelectedFgConv}, ConverterParameter=2}"
                                   Visibility="{Binding SelectedSettingIndex, Converter={StaticResource SelectedIndicatorConv}, ConverterParameter=2}"/>
                        <TextBlock Grid.Column="1" Text="Crosshair Dot" FontSize="9.5" FontWeight="SemiBold"
                                   Foreground="{Binding SelectedSettingIndex, Converter={StaticResource SelectedFgConv}, ConverterParameter=2}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding CrosshairStatusLabel, StringFormat=◄  {0}  ►}" FontSize="9.5" FontWeight="Bold"
                                   Foreground="{Binding SelectedSettingIndex, Converter={StaticResource SelectedFgConv}, ConverterParameter=2}"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Navigation Controls Instruction -->
                <Border Background="#0C121C" CornerRadius="4" Padding="6,2.5" BorderBrush="#1F2838" BorderThickness="1">
                    <TextBlock Text="[▲/▼] PILIH    [◄/►] UBAH"
                               FontSize="8.5" FontWeight="SemiBold" Foreground="#00FF88" HorizontalAlignment="Center"/>
                </Border>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
