<Window x:Class="PbRecoil.Views.CrosshairOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PB Crosshair"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowState="Normal"
        Left="0" Top="0"
        Width="100" Height="100"
        IsHitTestVisible="False">

    <Canvas x:Name="RootCanvas" Background="Transparent" ClipToBounds="False">
        <!-- Crosshair dot: lingkaran kecil 6x6 px berwarna merah terang dengan border halus -->
        <Ellipse x:Name="CrosshairDot"
                 Width="6" Height="6"
                 Fill="#FFFF3333"
                 Stroke="#80000000"
                 StrokeThickness="0.5"
                 Canvas.Left="0" Canvas.Top="0"/>
    </Canvas>
</Window>
